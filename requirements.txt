# Runtime extraction
numpy>=1.26

# Preprocessing pipelines
pandas>=2.2
scipy>=1.11
scikit-learn>=1.4
tqdm>=4.66

# CPU Leiden clustering/refinement
python-igraph>=0.11
leidenalg>=0.10
joblib>=1.3

# Optional GPU Leiden support is provided by RAPIDS packages such as
# cudf and cugraph. Install those from the RAPIDS/conda channels in a
# CUDA-specific environment rather than from this generic requirements file.
